# Playground

Local testing environment for sibyl-ts.

## Usage

```bash
# Install dependencies
npm install

# Run the test file
npm run dev

# Watch mode (auto-rerun on changes)
npm run watch
```

## What's Here

- `test.ts` - Your experiments and testing
- Automatically uses your local build from `../`

Edit `test.ts` to test your validators!
